import tkinter as tk

def dispLabel():
    lbl.configure(text="こんにちは")
root=tk.Tk()

root.geometry("300x200")
root.title("こんちにはアプリ")

lbl=tk.Label(text="ラベル")
lbl.place(x=120,y=120)
btn=tk.Button(text="押してください",command=dispLabel)
btn.place(x=100,y=50)

tk.mainloop()
